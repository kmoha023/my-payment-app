{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "Mock json server for testing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch SampleAppConfig-Call1.json --port 3004"
  },
  "author": "Khaja Mohammed",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
